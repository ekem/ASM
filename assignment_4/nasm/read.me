Less verbose output. But matchs the output of the masm program
for validation.

you can build and link by doing:

'./compile src/main'
'./link src/main'

and of course run it with
'./src/main'
after being built, or a compiled binary is in the root folder
